services:
  urmainpage:
    image: nginx:latest
    container_name: UrMainPage
    volumes:
     - ./templates:/etc/nginx/templates
    ports:
     - "80:80"
    env_file:
     - .env
    network_mode: host
    restart: on-failure

